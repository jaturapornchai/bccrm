/**
 * @bccrm/ui — shared components (เริ่มต้น)
 * Phase 1 ใช้ shadcn/ui ใน apps/admin โดยตรง; package นี้เก็บ component
 * ที่ใช้ข้าม admin + liff เมื่อมีของซ้ำจริง ๆ ค่อยย้ายมาที่นี่
 */

export { QueueStatusBadge } from "./components/QueueStatusBadge.js";
